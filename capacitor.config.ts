import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.soustudybuddy.app",
  appName: "sou-study-buddy",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
